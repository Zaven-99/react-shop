import styles from "./checkOut.module.scss";

//TODO checkout
const CheckOut = () => {
  return (
    <div className={styles["checkout-container"]}>
      <div className={styles["checkout-inner"]}>{}</div>
    </div>
  );
};

export default CheckOut;
